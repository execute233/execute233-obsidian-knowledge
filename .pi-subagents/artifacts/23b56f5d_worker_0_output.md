# Kotlin 表格灾难修复报告

## 范围
3 篇 Kotlin 笔记,共 6 张 GFM 表格被 OneNote 导出时插入独立 ```kotlin 栅栏切碎,导致表格列数不齐、GFM 解析失败。
任务要求:仅还原表格结构,保留所有代码片段与 wikilink,不动散文内容。

## 修改文件
1. `OneNote/kotlin/基础/基础知识.md`
2. `OneNote/kotlin/程序设计中级/封装，继承和多态.md`
3. `OneNote/kotlin/程序设计中级/类与对象.md`

## 每篇详细

### 1. `基础知识.md`
- **2 张表**,第 1 张 4 列(类型/大小/最小值/最大值)未受损;第 2 张 3 列(操作名/说明/示例)被 12 个独立栅栏切碎
- 表外 2 个孤立的栅栏块(`模版` 和 `$[变量]...`)被还原为纯文本行
- 改前约 50 行,改后 34 行
- 表行:9 行(2 张表 × 头/分隔/数据行)

### 2. `封装，继承和多态.md`
- **1 张表**(官方高阶扩展函数)3 列(定义/调用/说明),4 数据行被 15+ 栅栏切碎
- 表外 7 个伪标题栅栏(`**封装**`、`**继承**`、`**多态**` 等)不在任务范围内,保留原样
- 改前 ~140 行,改后 122 行
- 表行:6 行(头/分隔 + 4 数据行)

### 3. `类与对象.md`
- **4 张表**,全部还原:
  - 8 列基本运算符表(头/分隔 + 5 数据行 = 7 行)
  - 8 列比较/索引运算符表(头/分隔 + 4 数据行 = 6 行)
  - 4 列中缀函数优先级表(头/分隔 + 2 数据行 = 4 行)
  - 3 列访问权限表(头/分隔 + 4 数据行 = 6 行)
- 改前 369 行,改后 168 行
- 表行:23 行

## 合并策略
单行代码片段用反引号行内代码 `xxx` 包裹;代码片段塞入单元格,无需单独栅栏
(因为 GFM 表格单元格原生支持行内代码,且单行最自然)。表中示例
如 `a.unaryPlus()`、`0111 and 0011 = 0011`、`1 shl (2 + 3)` 等,
全部用 ` 反引号行内代码包裹,保持视觉整洁、表格可对齐。

伪标题栅栏(`**xxx**` 在 ```kotlin 块内) 不在任务范围内,保留原样,
这些属于"内容标题丢失"问题,不是表格问题;未来可单独走"标题重构"轮次处理。

## 验证

### git diff 统计
```
OneNote/kotlin/基础/基础知识.md                            | 73 +-------
OneNote/kotlin/程序设计中级/封装，继承和多态.md             | 32 +---
OneNote/kotlin/程序设计中级/类与对象.md                     | 193 ++-------------------
4 files changed, 28 insertions(+), 272 deletions(-)
```
(第 4 个文件 `.obsidian/copilot-index-*.json` 是 Obsidian 自动写入的索引日志,在 .gitignore 中)

### 栅栏残留验证
- 基础知识.md: 0 个开头 ``` 行(全部消除)
- 封装，继承和多态.md: 30 行(``` 开头的行) — 任务范围外,未触碰
- 类与对象.md: 32 行(``` 开头的行) — 任务范围外,未触碰

### 表格可对齐性
- 基础知识.md: 2 张表列数完全匹配
- 封装，继承和多态.md: 1 张表 3 列匹配
- 类与对象.md: 4 张表全部列数匹配(8/8/4/3 列,均头分隔数据列数一致)

### 工作树状态
- 干净(除 Obsidian 自动生成的 copilot-index 索引日志)
- 任务要求"不 commit",已遵守

## 残余风险
- 伪标题栅栏块(```kotlin **xxx** ```) 没动,后续应走"标题重构"轮次
- `基础知识.md` L8 表格内 `&nbsp；` HTML 实体未动(中英文分号混,语义上的拼写问题,非结构问题)
- 类与对象.md L101 `a.pulsAssign(b)` 拼写错误未动(原文如此,内容层)
- 类与对象.md L138 `a[I, j]` 大写 `I` 与上下文小写不一致(原文如此,内容层)
- 任务范围之外的散文代码示例块保持不变