✓ 6 篇 computer/ 已全部重写完毕。

---

## 总结报告

| 文件 | 修改点 |
|---|---|
| **将多数据压缩进字节** | 加 ## 单个字节存储多个 Bool;4 个 ### 函数说明;代码块内全角标点改半角;补 ## h2 总章 |
| **网络安全攻击** | 2 处 `**xxx**` → ## (CSRF/XSS); ### 子节 浏览器的保护机制;3 处 XSS 攻击类型前加 ** 粗体;语义补句号 |
| **HTTP协议** | 加 ## HTTP响应头部/HTTP版本/常见的HTTP响应码/SSE流式传输;5 个 ### 1xx-5xx 子节;HTTP 状态码全部转行内代码 `\`\`Bad Request\`\``(20 个);2 处孤立 `1. xxx` → ##;302 Found. / 429 ManyRequests. / 客户需应 / 请求少携带了 等错误已修正 |
| **TCP连接** | 加 ## 建立/关闭/头部信息/数据发送过程/常用解决 5 个章;编号跳号已修正(1-12 完整;9→8 等);全角 `1．`/`2．` → 半角;像想→想象;Sourcesize Port 等术语空格补齐 |
| **UDP协议** | 去除孤悬 `-`;User Datagram Protocol 放在代码注释内;加 ## 报文格式;巨的端囗 → 目的端口;分段逻辑整理 |
| **网络和通信协议** | 7 个 ### OSI 层(1.-7.);编号错乱修正;全角 `6．`/`7．` → 半角;常见协议列表加粗;传输层"端端端到端" → 端到端;ICMP/ARP/RARP 加粗;等等 |

**未动语义内容**(如 wikilink/URL/图片引用/原文叙述顺序)。

---