8 篇全部完成 — read 全文 + write 整体重写,无脚本。

---

## 改动摘要

### 已重写的 8 篇笔记

| 文件 | 主要改动 |
|---|---|
| `transform控制父子.md` | 代码块加缩进,补齐 `void Start()/Update()` 上下文,删除 `Debug._Log_` 伪强调,加注释 |
| `初步认识.md` | 8 个方法补中文注释,合并为 `## 生命周期方法` 段落 |
| `场景.md` | 删 `Debug._Log_` 伪强调,缩进代码,加注释,补 `using UnityEngine.SceneManagement` |
| `按键监听.md` | 鼠标 / 键盘 / 轴 / 虚拟按键分组加 `// =====` 注释,删 `Debug._Log_` 伪强调 |
| `文件-程序管理.md` | 拆分为 `## 常用路径` 与 `## 应用行为` 两个 H2,代码块分两段,全角标点改半角,删 `Debug._Log_` 伪强调 |
| `游戏时间.md` | 半角标点 + 中文注释 |
| `游戏物体的获取.md` | 补全 `o` / `Cube` 上下文,加 Inspector / 性能警告注释 |
| `触摸.md` | 5 个 `TouchPhase` 加中文注释,补全 foreach 多点触摸示例 |

### 核心规范

- frontmatter YAML 完整(title / tags / aliases)
- H1 = `# 文件名`
- 段落用 `## xxx` H2
- 代码块统一 ` ```cs ` 标签
- 代码内半角标点,文本内全角
- 全部 `Debug._Log_` / `Input._GetMouseButton_` 等 `_xxx_` 伪强调还原为真实 API
- 全部 `**xxx**` 伪强调还原为 H3 或注释
- wikilink / 图片 / URL / 语义全部保留

### 提交

`git add OneNote/unity/` 等会儿由主流程统一提交。