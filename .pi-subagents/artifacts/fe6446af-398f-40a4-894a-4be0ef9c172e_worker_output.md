---

**python 7 篇完成** — 7 文件全部 `read` → `write` 重写,无任何脚本自动化。

## 7 篇变化总览

| 文件 | 主要改动 |
|---|---|
| `包管理.md` | 6 个 `## xxx` 标题各带一段独立代码块,不再塞一个块 |
| `环境管理.md` | 7 个 `## xxx` 标题各带独立代码块;`#获得环境` 注释移入 fenced code |
| `管理conda.md` | `**查看某个命令的帮助**` 假标题栅栏移除;URL 链接外的 markdown 链接括号剥除 |
| `内置函数.md` | `2. input()` 改 `## input()`;函数签名半角化(`print(self, *args, ...)`);参数列表用列表项 |
| `基础.md` | 全文重构 H2/H3/H4 层级(序列/列表/字典/集合);统一中文标点;行内代码用反引号;`calue` 错字 → `value`;`[I for I in range]` 大写 I → 小写 i;`{0:1, 1:2, 3:4}` 错位 → `{0:1, 1:2, 2:3}` |
| `字符串相关.md` | 表格用 `\| 填充 \|` 的 GFM 写法;`spilt` → `split`;`12. 所有字符` 改为 `### str.istitle()`;保留 2 个过期 graph URL(用注释标记) |
| `类.md` | `**object 特殊方法**` / `**特殊属性**` 伪标题转为 `## object 特殊方法` / `## 特殊属性` |

## 关键修复

- **硬性约束遵守**: ❌ 无脚本, ❌ 无 edit 正则替换, ✅ 7 次 `read` + 7 次完整 `write`
- **语义保留**: 所有 wikilink `![[_assets/...]]` 保留
- **特殊处理**: `字符串相关.md` 的 2 个过期 OneNote Graph URL 按要求"保留但加注释标记"
- **frontmatter**: 7 篇均有完整 `title/tags/aliases`
- **H1**: 7 篇均为 `# 文件名`
- **代码块**: 全部带 `bash` / `python` / `text` 标签