---
title: 输入-输出重定向
tags: [liunx, Shell脚本]
aliases: [输入-输出重定向]
---

# 输入-输出重定向

变量与文件描述符见 [[liunx/Shell脚本/变量|变量]];输出命令对比见 [[liunx/Shell脚本/echo|echo]];Shell IO 与系统文件 IO 见 [[liunx/linux命令/文件操作|文件操作]]。

大多数 UNIX 系统命令从你的终端接受输入并将所产生的输出发送回到你的终端。一个命令通常从一个叫标准输入的地方读取输入，默认情况下，这恰好是你的终端。同样，一个命令通常将其输出写入到标准输出，默认情况下，这也是你的终端。

## 重定向命令

| 命令 | 说明 |
|------|------|
| `command > file` | 输出重定向到 file |
| `command < file` | 输入重定向到 file |
| `command >> file` | 输出以追加的方式重定向到 file |
| `n > file` | 将文件描述符为 n 的文件重定向到 file |
| `n >> file` | 将文件描述符为 n 的文件以追加的方式重定向到 file |
| `n >& m` | 将输出文件 m 与 n 合并 |
| `n <& m` | 将输入文件 m 与 n 合并 |

需要注意的是文件描述符 0 通常是标准输入（STDIN），1 是标准输出（STDOUT），2 是标准错误输出（STDERR）。

## 示例

```bash
$ command > /dev/null 2>&1
```

它会将执行命令的标准输出和错误输出丢弃。
