git config --global user.name "名字"
git config --global user.email 邮箱
git config --global http.proxy "[http://127.0.0.1:8080](http://127.0.0.1:8080)"
git config --global https.proxy "[http://127.0.0.1:8080](http://127.0.0.1:8080)"
创建git仓库
git init
git clone …
token鉴权(作为密码输入)
github -> setting -> developer settings -> personal access tokens -> classic令牌
ssh鉴权
cd ~/.ssh
ssh-keygen -t rsa -b 4096 -C 个人邮箱
生成对应的.pub公钥
github -> SSH and GPG keys 配置公钥