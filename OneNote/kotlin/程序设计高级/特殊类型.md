---
title: 特殊类型
tags: [kotlin, 程序设计高级]
aliases: [特殊类型]
---

# 特殊类型

## ```
    **数据类型**
    ```

    - 主构造函数中至少有一个参数
    - 主构造函数中的参数必须标记为val或var
    - 数据类不能是抽象的,开放的,密封的或内部的

可以手动重写equals()和hashCode(),但无法手动重写copy(),componentN()且会覆盖父类

```kotlin
**枚举类型**
```

使用enum修饰类,该类仅存储不同的多种状态,同java相似

```kotlin
**匿名类**
```

```kotlin
**单例类**
```

```kotlin
**伴生对象**
```

```kotlin
**委托模式**
```

使用data修饰类,该类仅存储数据,且对该类有以下要求:
同时编译器会自动生成以下函数
equals(), hashCode(), toString(), componentN(), copy()
注意,编译器只会根据主构造函数中定义的属性生成对应函数,如果有些时候我们不希望这些属性被添加到自动生成的函数中,我们需要手动将其移出主构造函数,如
```csharp
data class Person(val name: String) {
var age: Int = 0 // age不会被处理
```
}
对于enum对象,有几个属性和方法可以直接调用
```csharp
enum.name // 当前枚举的对象名
enum.ordinal // 当前枚举的排列位置,位置与索引相似
```
也可以自定义属性和方法,如
```csharp
enum class LightState(var color: String) {
RED("red"), YELLOW("yellow"), GREEN("green");
fun isGreen() = this == GREEN
```
}
对于类也有方法和属性
Enum.valueOf() // 以字符串名称的形式转换为对应名称的枚举
enumValueOf<Enum>() // 同上
Enum.entries // 获取所有枚举
使用object声明,如
```csharp
var obj = object[: 接口] {
fun test()
var age: Int = 18
```
} // 这样的类是没有构造函数的
同时,如果一个接口有fun修饰符,则它是个函数式接口,重写与java相似,如
```csharp
fun interface KRunnable {
fun invoke()
```
}
```csharp
fun main() {
var runnable = KRunnable { … }
```
runnable.invoke()
}
也比如
```csharp
fun main() {
test { … } // 直接现写调用
```
}
```csharp
fun interface Printer {
fun print()
```
}
fun test(printer: Printer) { // 需要Printer接口实现对象
printer.print()
}
单例类全局只能存在一个实例对象,其定义与类相似,但不能通过构造函数获取对象
```csharp
object Singleton { … }
fun main() {
var singleton = Singleton
```
}
既支持单例类那样调用,又支持普通class那样调用(单例类写在普通类内部),类似于java的静态调用,如
```csharp
class Student(val name: String, val age: Int) {
companion object Tools {
fun create(name: String, age: Int) = Student(name, age)
```
}
}
```csharp
fun main (
Student.create("xx", 18)
```
)
将构造操作委托给参数
```csharp
class Derived(var b: Base): Base by b
interface Base { fun print() }
class BaseImpl: Base { … }
fun main {
var base: Base = Derived(BaseImpl)
```
}
属性也可以委托
```python
import kotlin.reflect.Kproperty
class Example {
var p: String by Delegate
```
}
class Delegate {
// 需要重载属性的获取和设置
```text
operator fun getValue(thisRef: Any?, property: Kproperty<*>): String { … }
operator fun setValue(thisRef: Any?, property: Kproperty<*>, value: String): { … }
```
}
太麻烦了,可以使用预设的lazy,但只能对获取进行操作,对Example类修改为
```python
class Example {
var p: String by lazy { … }
```
}
属性也可以委托给另一个属性
```python
class Example<var a: String> {
var p: String by a
```
}
属性也可以委托给map
```csharp
class User(map: MutableMap<String, Any>) {
var name: String by map
var age: Int by map
```
}
```csharp
fun main() {
val map: MutableMap<String, Any> = mutableMapOF(
"name" to "xxx",
```
"age" to 18
）
val user = User（map）
}