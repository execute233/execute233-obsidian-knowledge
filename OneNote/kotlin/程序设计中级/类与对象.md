```
**类的定义**
```

与java类似,单独一个文件一个类

```
**构造函数**
```

除了主构造函数外，还可以有次构造函数，  
对象初始化  
在类里使用init关键字  
}

```
**运算符重载**
```

使用operator关键字指定方法实现，如  
}  
常见的运算符对应的函数名称

|   |   |   |   |   |   |   |   |
|---|---|---|---|---|---|---|---|
|符号|对应的函数名称|符号|对应的函数名称|符号|对应的函数名称|符号|对应的函数名称|
|+a|
```
a.unaryPlus()
```
a+b|
```
a.plus(b)
```
a..b|
```
a.rangeTo(b)
```
a-=b|
```
a.minusAssign(b)
```
|-a|
```
a.unaryMinus()
```
a-b|
```
a.minus(b)
```
a..\<b|
```
a.rangeUntil(b)
```
a*=b|
```
a.timesAssign(b)
```
|!a|
```
a.not()
```
a*b|
```
a.times(b)
```
a in b|
```
a.contains(b)
```
a/=b|
```
a.divAssign(b)
```
|a--|
```
a.dec()
```
a/b|
```
a.div(b)
```
a !in b|
```
!a.contains(b)
```
a%=b|
```
a.remAssign(b)
```
|a++|
```
a.inc()
```
a%b|
```
a.rem(b)
```
a+=b|
```
a.pulsAssign(b)
```
||

重载些比较运算符,甚至是小括号,索引运算符

|   |   |   |   |   |   |   |   |
|---|---|---|---|---|---|---|---|
|运算符|对应的函数名称|运算符|对应的操作|运算符|对应操作|运算符|对应操作|
|a\>b|
```
a.compareTo(b) \> 0
```
```
a()
```
```
a.invoke()
```
a[i]|
```
a.get(i)
```
a[i] = b|
```
a.set(i, b)
```
|a\<b|
```
a.compareTo(b) \< 0
```
```
a(i)
```
```
a.invoke(i)
```
a[I, j]|
```
a.get(I, j)
```
a[I, j] = b|
```
a.set(i, j, b)
```
|a\>=b|
```
a.compareTo(b) \>= 0
```
a(a, j)|
```
a.invoke(I, j)
```
a[i_1,..,i_n]|
```
a.get(i_1,…,i_n)
```
a[i_1,..,i_n] = b|
```
a.set(i_1,…,i_n,b)
```
|a\<=b|
```
a.compareTo(b) \<= 0
```
a(i_1,…,i_n)|
```
a.invoke(i_1,..,i_n)
```
||||

```
**中缀函数**
```

使用时可以省略调用的点和括号，使用infix关键字修饰函数，且该函数必须是成员函数，只有一个参数，参数没有默认值  
}  
}  
中缀函数的运算优先级低于算数运算符，类型转换和rangeTo，但高于逻辑运算符，如

|   |   |   |   |
|---|---|---|---|
|表达式1|等价于表达式1的表达式|表达式2|等价于表达式2的表达式|
```
1 shl 2 + 3
```
```
1 shl (2 + 3)
```
```
a && b xor c
```
```
a &&(b xor c)
```
```
0 until n * 2
```
```
0 until (n * 2)
```
```
a xor b in c
```
```
(a xor b) in c
```

```
**空值和空类型**
```

可以使用?: (Elvis运算符)自定义为空时的返回结果  
var str: String = null  
val len: Int = str?.length ?: 0

```
**解构声明**
```

```
**包和导入**
```

跟java一样，但多了as可以改名(类似于python)

```
**访问权限控制**
```

在类，对象，接口，构造函数和函数以及属性上都可使用，默认public，有以下修饰符

|   |   |   |
|---|---|---|
|修饰符|作用|备注|
```
public
```
任何地方可访问|默认修饰符|
```
private
```
当前文件可访问||
```
internal
```
同一模块中可见||
```
protected
```
仅子类可用|顶级声明不可用|

在类名构面加个小括号，如  
calss Student(var name: String, var age Int [= ???]) {}  
// 这样指定了必有的属性  
要添加访问修饰符，要  
class Student private constructor(name String, age Int) {}  
要自定义属性的get，set，可以这样  
class Student(name: String, age: Int) {  
var name:String  
get(){}  
set(){}  
}  
不想要立马初始化属性，可以用lateinit懒加载  
class Student() {  
lateinit var name: String  
}  
如果该类有一个主构造函数，则每个次构造函数要通过另一个次构造函数直接或间接地委托给主构造函数  
class student(var name: String, var age: Int) {  
construstor(name: String) : this(name, 18)  
}  
class Student {  
init {  
初始化代码..  
}  
operator fun plus(another: Student): Student.. {  
return Student(..)  
class Student(var name: String, var age: Int) {  
infix fun text(string: String) : String{  
return name + string  
}  
fun main() {  
var stu = Student("xxx", 18)  
println(stu text "xxx")  
kotlin不允许变量为null，只有类型后加?为可空类型，这样后续操作可空变量需检查，如  
val stu: Student? = null  
if (stu != null) {  
stu.name // 这里判断了非空，Student?自动转换为Student类型  
}  
若明确该变量不为空，需!!强制使用，如  
stu!!.name // 可能会出现NPE  
但有更安全的操作，在后面用?,变量引用为空时返回null，如  
var str: String? = null  
println(str?.length)  
访问对象里的属性，可以优雅地将其内部属性解构出来，如  
val stu = Student("xxx", 18)  
val (a, b) = stu // 直接把属性取出来，很优雅  
println("name: $a, age: $b")  
要实现这样的结构,需要添加约定函数，定义不定个componentN函数并通过赋值的方式返回结构的结果,如  
class Student(var name: String, var age: Int) {  
operator fun component1() = name // 解构的第一个参数  
operator fun component2() = age // 解构的第二个参数  
operator fun component3… // 以此类推  
}  
它也可以在lambda中解构，如  
val func1: (Student, Int) -\> Unit = {(name, age), num -\>  
语句..  
}