---
title: 基础语法
tags: [kotlin, 基础]
aliases: [基础语法]
---

# 基础语法

1. **声明变量**

var [变量名称]： [变量类型] = [值]
可写var a = 10编译器会**自动推断**类型

```kotlin
**顶层变量**(定义在文件里)
```
 5. **声明常量**

val [变量名称]： [变量类型] = [值]
可写val a = 10编译器会自动推断类型

7. **条件判断**

这里的if内仍可嵌套代码，默认**最后一行作为返回的结果**

9. **选择结构**（类似于switch）

```kotlin
同时,**与 if 一样可返回值**
```

```kotlin
**循环**(类似于python)
```

```kotlin
**起别名**
```

```xml
var\<propertyName\>[\<PropertyType\>][= \<property-initializer\>]
[\<getter\>]
[\<setter\>]
如
var a: Int = 5
get() {
field * 10
}
set(value) {
field = value // field是a变量,vaule是赋值的变量
}
```

```kotlin
跟java类似,但也**可做结果返回**,如
//这里判断socre是否大于60,是就得到Yes,否就得到No,并且可以直接赋值给变量
var res = if(score\>60)"Yes"else"NO"
when (目标) {
匹配值1,匹配值2… -\> 代码…
匹配值3,匹配值4… -\> 代码…
else -\> {
代码…
}
}
也可使用任意表达式作为分支条件,如
val score = XXX
val grade = when(score) {
in 100..90 -\> "优秀"
in 89..80 -\> "良好"
…
else -\> "不及格"
}
for (遍历的单个变量 in 可遍历目标 [step 步长) {
语句..
}
也可以反过来遍历,如
for (i in 10 downTo 1 [step 步长) {
语句…
}
同时也可用
while (循环条件) {
语句..
}
使用typealias关键字给类型起别名,如:
typealias Test = () -\> Unit // Test类型是一个无参数列表且无返回值的函数
```