---
title: 单机容器编排
tags: [docker]
aliases: [单机容器编排]
---

# 单机容器编排

## 快速开始

安装插件：

```bash
apt install docker-compose-plugin
```

IDEA 配置：配置成自己的 `docker-compose` 可执行文件路径。

![[_assets/单机容器编排/单机容器编排__09-14-50-0.png]]

然后编写 `docker-compose.yml` 文件：

```yaml
version: "3.9"  # docker 对应的版本号
```