---
title: 配置与运行
tags: [docker, docker]
aliases: [配置与运行]
---

# 配置与运行

**配置与运行**
分为CE与EE版本，教程在[Ubuntu | Docker Doc](https://docs.docker.com/engine/install/ubuntu)
默认2375端口

代理配置
方式一：
```bash
sudo vim /etc/docker/daemon.json
{
```
"proxies"： {
```json
"http-proxy": "[http://127.0.0.1:7890](http://127.0.0.1:7890)",
"https-proxy": "[http://127.0.0.1:7890](http://127.0.0.1:7890)",
"no-proxy": "localhost,127.0.0.1"
```
}
}
sudo systemctl restart docker
方式二：
sudo systemctl status docker

```json
[Service]
Environment="HTTP_PROXY=http://127.0.0.1:7890"
Environment="HTTPS_PROXY=http://127.0.0.1:7890"
Environment="NO_PROXY=localhost,127.0.0.1"
```

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```