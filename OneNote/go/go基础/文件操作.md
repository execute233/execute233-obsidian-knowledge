---
title: 文件操作
tags: [go, 文件操作, IO]
aliases: []
---

# 文件操作

## 打开和关闭文件

`os.Open()` 函数能够打开一个文件,返回一个 `*File` 和一个 `err`。对得到的文件实例调用 `close()` 方法能够关闭文件。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-16-0.png)

为了防止文件忘记关闭,通常使用 `defer` 注册文件关闭语句。

## 读取文件

```go
func (f *File) Read(b []byte) (n int, err error)
```

接收一个字节切片,返回读取的字节数和可能的具体错误,读到文件末尾时会返回 `0` 和 `io.EOF`,如:

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-21-1.png)

可以使用 `bufio` 读取,它在此封装了一层 API,支持更多功能:

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-23-2.png)

如果要读取整个文件,可以使用 `os.ReadFile`。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-25-3.png)

## 写入文件

`os.OpenFile()` 函数能够以指定模式打开文件,从而实现文件写入相关功能。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-26-4.png)

参数说明:

- `name`:要打开的文件名
- `flag`:打开文件的模式,模式有以下几种:

| 模式 | 说明 |
| --- | --- |
| `os.O_WRONLY` | 只写 |
| `os.O_CREATE` | 创建文件 |
| `os.O_RDONLY` | 只读 |
| `os.O_RDWR` | 读写 |
| `os.O_TRUNC` | 清空 |
| `os.O_APPEND` | 追加 |

- `perm`:文件权限,一个八进制数。r(读)04,w(写)02,x(执行)01。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-28-5.png)

我们还可以通过 `os.WriteFile` 直接向文件写入内容。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-30-6.png)

还可以用带缓冲区的 `Writer`。

![Exported image](_assets/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C__13-01-31-7.png)
