Redi是一个开源的键值存储数据库，所有的数据全部存放在内存中，它的性能大大高于磁盘IO，并且它也可以支持数据持久化，他还支持横向扩展、主从复制等。  
安装  
linux直接sudo apt install redis-server  
windows网上找安装包解压即用,redis-server.exe是启动  
配置外网访问  
配置文件删除 bind 127.0.0.1  
potected-mode改为false