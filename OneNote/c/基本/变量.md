1. **变量的声明与使用**

同java一样，略

3. **格式控制符**
4. **占位符详细控制**
    - flags:
    - width:
    - .precision(精度):
    - specifeier:
5. **常量**

使用const关键字声明常量

7. **无符号数**

使用unsigned修饰变量，变为无符号数

9. **类型转换**

不同类型数据进行运算，会转为高优先级的数据进行运算

![Exported image](_assets/%E5%8F%98%E9%87%8F/%E5%8F%98%E9%87%8F__11-43-01-0.png)  
13. 不同的类型优先级不同（根据长度而定）
14. char和short类型在参与运算时一律转换为int再进行运算。
15. 浮点类型默认按双精度进行计算，所以就算有float类型，也会转换为double类型参与计算。
16. 当有一个更高优先级的类型和一个低优先级的类型同时参与运算时，统一转换为高优先级运算

可以使用强制类型转换，使用方法与java一样

18. **运算符**
19. **位运算符**
20. **逻辑运算符**

逻辑与&&, 逻辑或||, 取反!

22. **三元运算符**

同java一样

24. **运算符优先级**

使用printf()可用格式控制符来打印变量，如  
int a = 1;  
printf("result: %d", a)  
还以下的格式控制符

|   |   |
|---|---|
|%c|输出一个单一的字符|
|%hd, %d,%ld|以十进制、有符号的形式输出short、int、long类型的整数|
|%hu, %u, %lu|以十进制、无符号的形式输出short、int*long类型的整数|
|％ho, %o,%lo|以八进制、不带前缀、无符号的形式输出short、int、long类型的整数|
|%hx, %x, %lx, %hX, %X, %lX|以十六进隔、不带前缀、无符号的形式输出short、int、long类型的整数。如果x~小写，那么输出的十六进制数字也小写；如果×大写，那么输出的十六进制数字也大写。|
|%#hx, %#x, %#lx, %#hX, %#X, %#lX|以十六进制、带前缀、无符号的形式输出short、int、long类型的整数。如果x小写，那么输出的十六进制数字和前缀都小写；如果×大写，那么输出的十六进制数字和前缀都大写。|
|%f, %lf|以十进制的形式输出float、double类型的小数|
|%e, %le, %E, %lE|以指数的形式输出float、double类型的小数。如果e小写，那么输出结果中的e也小写；如果E大写，那么输出结果中的E也大写。|
|%g, %lg, %G, %lG|以十进制和指数中较短的形式输出float、double类型的小数，并且小数部分的最后不会添加多余的0。如果g小写，那么当以指数形式输出时e也小写；如果G大写，那么当以指数形式输出时E也大写。|
|%s|输出一个字符串|

%[flags][width][.precision]specifier

![Exported image](_assets/%E5%8F%98%E9%87%8F/%E5%8F%98%E9%87%8F__11-43-03-1.png) ![Exported image](_assets/%E5%8F%98%E9%87%8F/%E5%8F%98%E9%87%8F__11-43-04-2.png)

对于字符串，是打印字符的最大个数  
对于整数，是打印数字的位数  
对于e, E, 或f,指定打印小数点后的数字位数  
对于g或G，是打印的有效数字位数  
就上面格式控制符%后面的(几个)字母
 
c中只有这些数学运算符(没有乘方运算)  
加法+,减法-,乘法*,除法/,取余数%  
也有逗号运算符，表示从前往后依次执行，如  
int b, c  
int a = (b = 5, c = b + 8) // a的值为13  
自增自减运算符  
跟java一样  
位移运算符  
在二进制层面上左移(\<\<)或右移(\>\>),如  
a \<\<= 1 // a自身左移1位(乘2的1次方)  
位比较运算符  
按位与&, 按位或|, 按位异或^, 按位非~

|   |   |
|---|---|
|()|括号，直接升到最高优先级|
|! ~ ++ -- + -|否定 按位否定 自增 自减 正负号 间接 取地址 类型转换|
|* / %|乘 除 取模|
|+ -|加 减|
|\<\< \>\>|左移 右移|
|\< \<= \>= \>|小于 小于等于 大于等于 大于|
|== !=|等于 不等于|
|&|按位与|
|^|按位异或|
|\||按位或|
|&&|逻辑与|
|\||逻辑或|
|?:|条件|
|= += -= *= /= &= ^= \|= \<\<= \>\>=|各种赋值|
|,|逗号(顺序)|
