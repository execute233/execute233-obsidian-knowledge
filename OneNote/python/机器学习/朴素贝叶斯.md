---
title: 朴素贝叶斯
tags: [机器学习, 朴素贝叶斯, 分类, 概率, 文本分类]
aliases: []
---

# 朴素贝叶斯

监督学习分类算法之一（参见 [[概述]] 中的算法分类）。利用概率值进行分类。一般使用贝叶斯公式。

$$P(C|W) = \frac{P(W|C) P(C)}{P(W)}$$

- $P(C)$ 表示 C 出现的概率，一般是目标值
- $P(W|C)$ 表示 C 条件下 W 出现的概率
- $P(W)$ 表示 W 出现的概率

原始贝叶斯是：

$$P(AB) = P(A) P(B|A) = P(B) P(A|B)$$

加入朴素后：

$$P(AB) = P(A) P(B)$$

## 拉普拉斯平滑系数

为了避免概率值为 0，分子分母分别加上一个数值。

$$P(F_1, C) = \frac{N_i + \alpha}{N + \alpha m}$$

- $\alpha$ 为拉普拉斯平滑系数，一般为 1
- $N_i$ 是 $F_1$ 中符合条件 C 的样本数量
- $N$ 是在条件 C 下所有样本的总数
- $m$ 表示说有独立样本的总数

## 代码示例

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import jieba
from sklearn.feature_extraction.text import CountVectorizer  # 词频统计包，把评论内容转为词频矩阵
from sklearn.naive_bayes import MultinomialNB  # 朴素贝叶斯

data = pd.read_csv('./data/书籍评价.csv', encoding='gbk')
data['label'] = np.where(data['评价'] == '好评', 1, 0)
y = data['label']
comment_list = [','.join(jieba.lcut(line)) for line in data['内容']]  # 对评论内容做切词

# 加载停用词列表，里面记录的词不参与训练
with open("./data/stopwords.txt", "r", encoding="utf-8") as f:
    stopwords = [line.strip() for line in f.readlines()]
stopwords = list(set(stopwords))  # 停用词去重

# 创建向量化对象，从 comment_list 删除停用词
transfer = CountVectorizer(stop_words=stopwords)
transfer.fit(comment_list)  # 统计词频矩阵，训练
x = transfer.transform(comment_list).toarray()  # 统计词频矩阵
transfer.get_feature_names_out()

x_train = x[:10]
y_train = y[:10]
x_test = x[10:]
y_test = y[10:]

estimator = MultinomialNB()  # 朴素贝叶斯对象
estimator.fit(x_train, y_train)
y_pred = estimator.predict(x_test)
```
