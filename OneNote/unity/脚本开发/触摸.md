---
title: 触摸
tags: [unity, 脚本开发]
aliases: [触摸]
---

# 触摸

触摸输入是 [[unity/脚本开发/按键监听|按键监听]] 的多点版本,处理都在 [[unity/脚本开发/初步认识|初步认识]] 所述的 Update 中。触摸后移动 transform 见 [[unity/脚本开发/transform控制父子|transform控制父子]]。

```cs
void Start()
{
    // 开启多点触摸
    Input.multiTouchEnabled = true;
}

void Update()
{
    // 判断单点触摸
    if (Input.touchCount == 1)
    {
        Touch touch = Input.touches[0];
        Debug.Log(touch.position); // 触摸位置(屏幕坐标)

        // 触摸阶段
        switch (touch.phase)
        {
            case TouchPhase.Began:
                // 手指刚触碰屏幕
                break;
            case TouchPhase.Moved:
                // 手指在屏幕上移动
                break;
            case TouchPhase.Stationary:
                // 手指在屏幕上但未移动
                break;
            case TouchPhase.Ended:
                // 手指离开屏幕
                break;
            case TouchPhase.Canceled:
                // 触摸被系统事件打断(如来电)
                break;
        }
    }

    // 判断多点触摸
    if (Input.touchCount >= 2)
    {
        foreach (Touch touch in Input.touches)
        {
            // 多点处理:遍历每个触点
        }
    }
}
```
