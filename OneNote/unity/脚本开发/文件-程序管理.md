---
title: 文件-程序管理
tags: [unity, 脚本开发]
aliases: [文件-程序管理]
---

# 文件-程序管理

## 常用路径

```cs
// 游戏数据文件夹路径,即项目里的 Assets 文件夹;打包后是加密、压缩、只读的
Debug.Log(Application.dataPath);

// 持久化文件夹路径,由系统分配,可在不同平台之间持久保存数据
Debug.Log(Application.persistentDataPath);

// StreamingAssets 路径,只读且不加密,常用于存放原始资源
Debug.Log(Application.streamingAssetsPath);

// 临时文件夹路径
Debug.Log(Application.temporaryCachePath);

// 是否允许在后台运行
Debug.Log(Application.runInBackground);
```

## 应用行为

```cs
// 用默认浏览器打开 URL
Application.OpenURL("https://www.google.com/");

// 退出游戏(在编辑器中不生效,仅在打包后有效)
Application.Quit();
```