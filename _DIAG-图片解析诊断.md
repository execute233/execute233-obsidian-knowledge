# 图片解析诊断

> ⚠ 这次没用代码块包裹,真实渲染测了
> 给 @AI 反馈:**哪几条图片实际显示了**?失败的会自动显示"找不到..."

**目标文件**(任意一张):  
`OneNote/java/设计模式/.attachments/创建类型/创建类型__09-27-04-0.png` (206 KB)

---

## 格式 ① — 笔记-相对路径,带 `.attachments/`(当前已失败的形式)

![[.attachments/创建类型/创建类型__09-27-04-0.png]]

---

## 格式 ② — 笔记-相对路径,显式 `./` 开头

![[./.attachments/创建类型/创建类型__09-27-04-0.png]]

---

## 格式 ③ — vault 根绝对路径

![[OneNote/java/设计模式/.attachments/创建类型/创建类型__09-27-04-0.png]]

---

## 格式 ④ — 裸名(全局搜索同名,理论上唯一)

![[创建类型__09-27-04-0.png]]

---

## 格式 ⑤ — 笔记-相对路径,把目录改成 `_assets`

> ⚠ **这一条需要先把磁盘上的 `.attachments/` 改成 `_assets/` 才有效**。
> **先告诉 AI 结果,等 AI 同意时再说要不要 mv**,否则目录还没改这条一定失败。

这里**先占位**:如果 Obsidian 因为目录不存在已经显示"找不到",可以**忽略这格式**;等下批再测。

(临时占位文本)

---

## 格式 ⑥ — 反斜杠(Windows 习惯,大概率失败)

(略,与 ① 等价但用反斜杠;Obsidian 通常只认 `/`)

---

## 你需要做的

1. Ctrl+P → "Reload app without saving" 强制重建索引
2. 打开本笔记,**向下滚动**到格式 ① ② ③ ④ 区域
3. 文本告诉我:哪几张**真实显示了图**(看到了 206KB 的图片内容)
4. 不要看"链接图标",要看到图片**实体**

可能的预期:
- 如果 ① 显示 → `.attachments/` 完全没问题,问题只是刷新滞后,所有 java 笔记刷新一下就好
- 如果 ③ 显示但 ① 不显示 → `.attachments/` 路径有 bug,需要改用绝对路径或换名
- 如果 ③④ 都显示但 ① 不显示 → 多半是 dotfile 索引问题,改成 `_assets/` 最稳
- 如果只有 ② 显示 → 路径前缀要显式 `./`

---

## 上一版为什么看着都"成功"

我把 wikilink 写进了 ``` 代码块里,Obsidian 把它当纯文本,**根本没去解析**。
上一张截图里那个"链接图标"是 Obsidian 给代码块的复制按钮。
