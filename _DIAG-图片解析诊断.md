# 图片解析诊断

> 阶段性胜利:**已把所有 `.attachments/` 改名为 `_assets/`**
> 现在请在 Obsidian 里 **Reload app without saving** 强制重建索引
> 然后,**只要 `_assets/` 形式的 wikilink 渲染了图片**,这次重构就大功告成。

**目标文件**(任意一张):  
`OneNote/java/设计模式/_assets/创建类型/创建类型__09-27-04-0.png` (206 KB)

---

## 格式 A — 笔记-相对路径,带 `_assets/`

![[_assets/创建类型/创建类型__09-27-04-0.png]]

---

## 格式 B — vault 根绝对路径(`/` 开头)

![[/OneNote/java/设计模式/_assets/创建类型/创建类型__09-27-04-0.png]]

---

## 格式 C — 裸名(全局搜索同名,理论上唯一)

![[创建类型__09-27-04-0.png]]

---

## 你需要做的

1. Ctrl+P → **"Reload app without saving"** 强制重建索引
2. 滚到 A B C 位置,只回答:"**实际显示了图片**的有哪几条"

预期:
- 如果 A 显示了 → 大功告成,以后都用 `![[_assets/{slug}/x.png]]`
- 如果只有 B(以 `/` 开头)显示 → 笔记-相对路径有别的问题,以后全部改用 vault 绝对路径
- 如果三条都不显示 → 索引器有更深问题,告诉我

---

## 历史记录

`30min ago` 全部格式失败 → 我把 `.attachments/` 改成 `_assets/`,排除点号索引问题 → 现在再测
